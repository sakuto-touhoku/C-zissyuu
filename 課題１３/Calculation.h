#pragma once

class Calclation {
private:
    float a;
    float b;

public:
    // コンストラクタ（必要に応じて）
    // Calclation();

    // 変数 a のセッター関数
    void SetA(float value);

    // 変数 b のセッター関数
    void SetB(float value);

    // 計算と結果表示の関数
    void Disp();
};